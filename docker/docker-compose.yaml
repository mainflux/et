version: "3.8"

services:
  timescaledb:
    image: timescale/timescaledb:latest-pg14
    container_name: timescaledb
    restart: on-failure
    env_file:
      - .env
    ports:
      - 5437:${MF_TIMESCALE_PORT}
    volumes:
      - timescaledb-data:/var/lib/postgresql/data
    environment:
      POSTGRES_PASSWORD: ${MF_TIMESCALE_PASSWORD}
      POSTGRES_USER: ${MF_TIMESCALE_USER}
      POSTGRES_DB: ${MF_TIMESCALE_DB_NAME}
    networks:
      - et-base-net
  et-server:
    image: mainflux/call-home:${ET_RELEASE_TAG}
    container_name: et-server
    depends_on:
      - timescaledb
    restart: on-failure
    env_file:
      - .env
    ports:
      - ${ET_PORT}:${ET_PORT}
    networks:
      - et-base-net
    volumes:
      - ./creds.json:/creds.json
      - ./IP2LOCATION-LITE-DB5.BIN:/IP2LOCATION-LITE-DB5.BIN
networks:
  et-base-net:
    driver: bridge
volumes:
  timescaledb-data: